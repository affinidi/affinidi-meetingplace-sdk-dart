/// JSON-LD context and type values for liveness credentials.
abstract final class LivenessCredentialConstants {
  /// Verifiable Credential type used for liveness claims.
  static const typeLivenessCredential = 'LivenessCredential';

  /// JSON-LD context for liveness credentials.
  static const contextLivenessCredential =
      'https://schema.affinidi.io/LivenessCredentialV1R0.jsonld';
}
