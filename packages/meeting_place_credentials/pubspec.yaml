name: meeting_place_credentials
description: Verifiable Relationship Credential (VRC), Relationship Card (R-Card), and liveness credential domain models, builders, and repository interfaces for the Meeting Place SDK.
version: 0.0.1-dev.6
repository: https://github.com/affinidi/affinidi-meetingplace-sdk-dart.git
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  affinidi_tdk_vdip: ^2.0.3
  json_annotation: ^4.9.0
  meeting_place_core: ^0.0.1-dev.41
  retry: ^3.1.2
  ssi: ^3.3.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.9.5
  mocktail: ^1.0.4
  test: ^1.26.2
