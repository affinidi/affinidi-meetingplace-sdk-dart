include: package:dart_flutter_team_lints/analysis_options.yaml

linter:
  rules:
    - avoid_print
    - sort_constructors_first
    - unawaited_futures

analyzer:
  errors:
    avoid_print: error
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
