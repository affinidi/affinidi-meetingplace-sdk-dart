/// The role a party plays in a VRC exchange.
enum VrcExchangeRole {
  /// The party who initiates the VRC exchange.
  initiator,

  /// The party who responds to the VRC exchange request.
  responder,
}
