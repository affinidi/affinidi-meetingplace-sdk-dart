name: meeting_place_relationship
description: Verifiable Relationship Credential (VRC) and Relationship Card (R-Card) domain models, builders, and repository interfaces for the Meeting Place SDK.
version: 0.0.1-dev.1
repository: https://github.com/affinidi/affinidi-meetingplace-sdk-dart.git
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  json_annotation: ^4.9.0
  meeting_place_core: ^0.0.1-dev.31
  ssi: ^3.3.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.9.5
  mocktail: ^1.0.4
  test: ^1.26.2
